export interface Student {
  id: number;
  name: string;
  role: string;
  bio: string;
  imageUrl: string;
}

export interface GalleryImage {
  id: number;
  url: string;
  title: string;
  description: string;
}